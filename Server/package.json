{
  "name": "dailyvault-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DailyVault MCP and HTTP access layer over the Markdown vault.",
  "scripts": {
    "mcp": "node src/mcp/server.js",
    "http": "node src/http/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/mcp/server.js && node --check src/http/server.js && node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
