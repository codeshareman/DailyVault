# AGENTS.md

DailyVault 是一个 Daily-first 的个人每日记录库。`YYYY/YYYY-MM-DD.md` 是唯一的日常事实来源；计划、输入、输出、生活片段、学习和复盘都直接写在当天页面。Dataview 和周期复盘只从每日页面派生，不要求维护第二套资料元数据。

## 核心规则

1. 不要编造事实、任务、情绪、决定、输出、地点或完成状态。
2. 除非用户明确要求改写，否则保留每日记录中的用户原话。
3. 优先追加摘要、分析或下一步建议，不覆盖原始笔记。
4. 每日记录只进入对应年份的顶层目录；仅在跨入新年时创建新的 `YYYY/` 目录。
5. 不要为文章、工具、链接、地点或训练创建 Source 记录。将其写入当天的“输入”或“生活时间线”。
6. 剪藏保留在 `Clippings/`；Daily 只链接剪藏并写明当天的关联或判断。
7. 不确定结论标为 `[推断]`。
8. 除非用户明确要求，否则不要删除、归档、发布或导出用户笔记。
9. 章节标题是 Dataview 统计的标识契约：今日计划、随手记录、输入、输出、生活时间线、学到、复盘、明日 / 迁移 必须与 `Templates/daily.md` 完全一致，不要改名、加前缀或加空格；行尾 `#kind/...` 标签是细粒度标识，用于文章、工具、课程等类型的分类统计。

## 活跃目录

- `YYYY/`：每个年份一个顶层目录；每日记录直接存放于 `YYYY/YYYY-MM-DD.md`，周期计划与复盘（一期一文件）位于 `YYYY/{Weekly,Monthly,Quarterly,Yearly}/`，年度统计文件 `YYYY/Yearly/{YYYY} 统计.md` 在创建年度文件时自动生成。
- `Clippings/`：Obsidian Web Clipper 的原文快照。
- `Templates/`：Daily 和周期复盘模板。

`.dailyvault/openapi/` 是独立的本地 HTTP/MCP 支持模块；除非任务涉及 API，不要改动其中代码。

## 日常工作流

### 每日记录

QuickAdd 只提供“创建今天”和“追加到今天”。当天记录保持自然语言，使用固定章节：今日计划、随手记录、输入、输出、生活时间线、学到、复盘、明日 / 迁移。

- 今日计划：最多三项可勾选行动。
- 随手记录：白天的临时捕捉；不要求即时分类。
- 输入：文章、工具、对话、课程和反馈等外部信息，行尾带 `#kind/...` 类型标签（模板注释有速查）。
- 输出：完成的交付物、决定或明确结果。
- 生活时间线：当天经历的活动和片段，可带 `#kind/fitness`、`#kind/place` 等标签。
- 学到：从输入或经历中提炼出的认识。
- 复盘：对行动过程的评价；明天的动作放到“明日 / 迁移”。

同一事实只写一次，按其对当天最有用的意义放入一个章节。

### 每日收尾

当用户要求“daily 总结 / 收尾 / 复盘 / closeout”时，先读相应的 `YYYY/YYYY-MM-DD.md`；只有每日记录中有明确的 Clippings 链接时才读取对应剪藏。输出今日主线、输入、输出、计划完成情况、生活片段、学到、复盘、迁移项和明日最重要三件事建议。没有证据时写“未观察到”。

### 周期计划与复盘（一期一文件）

周期文件位于 `YYYY/{Weekly,Monthly,Quarterly,Yearly}/`，**一个周期一个文件**，同时承载计划与复盘，对齐 Daily 的「今日计划 → 复盘」模式：

- **期初**：QuickAdd「创建周/月/季/年计划与复盘」创建 `2026-W34.md` / `2026-08.md` / `2026-Q3.md` / `2026.md`，Templater 自动填日期 frontmatter，先填「本期计划」（≤3 项可勾选）。
- **期内**：把「本期计划」拆解到各天「今日计划」，计划不需要重复维护。
- **期末**：在同一文件补「本期总结」与「下期重点」（下期重点即下一期计划，≤3 项）。

周期文件固定章节：本期计划、本期总结、下期重点（命名与模板一致，不要改名）。**统计不内嵌在周期文件里**：周/月/季模板只保留一行指向年度统计的外链（如 `[[2026 统计]]`）；年度模板在创建时由 Templater 自动生成 `YYYY/Yearly/{YYYY} 统计.md`。

复盘以 `YYYY/YYYY-MM-DD.md` 为唯一证据源。年度统计文件（`note_type: stats`）用自身的 `year` frontmatter 匹配每日页面做全年统计：章节条目统计、每日活跃度、计划完成情况（今日计划 checkbox）、未完成任务、`#kind/...` 条目分类、年度剪藏输入（按 `Clippings/` 的 `created` 字段归集）。统计只做年度一次；周期粒度的细节回看翻每日记录或由复盘摘录。不要从正文推断未记录的状态或指标。

生成有两条路径：

- **半自动**：期初 QuickAdd「创建周/月/季/年计划与复盘」建一期一文件，用户填「本期计划」；期末 AI 或用户在同一文件补「本期总结」「下期重点」。年度统计随年度文件创建时自动生成，其他周期链接到年度统计。
- **全自动**：调用 `period-review` skill，读取该周期全部每日记录后生成含总结内容的完整复盘，写入对应的一期一文件（不存在则按模板创建）。周期归属由文件 frontmatter 的 `week`/`month`/`quarter`/`year` 决定，创建时必须填准。

## 编辑策略

1. 只读取与任务相关的每日记录、模板、剪藏或周期复盘文件。
2. 修改模板后，确认 QuickAdd 的“追加到今天”仍定位到 `## 随手记录`。
3. 修改 JSON 配置后，确认仍能解析。
4. 不改写历史每日记录，也不恢复已退役的 Source/Note 默认流程。
