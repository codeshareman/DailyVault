# AGENTS.md

DailyVault 是一个 Daily-first 的个人每日记录库。`Daily/YYYYMMDD.md` 是唯一的日常事实来源；计划、输入、输出、生活片段、学习和复盘都直接写在当天页面。Dataview 和周期复盘只从 `Daily/` 派生，不要求维护第二套资料元数据。

## 核心规则

1. 不要编造事实、任务、情绪、决定、输出、地点或完成状态。
2. 除非用户明确要求改写，否则保留 `Daily/` 中的用户原话。
3. 优先追加摘要、分析或下一步建议，不覆盖原始笔记。
4. 不创建新的顶层目录；新记录默认只进入 `Daily/`。
5. 不要为文章、工具、链接、地点或训练创建 Source 记录。将其写入当天的“输入”或“生活时间线”。
6. 剪藏保留在 `Clippings/`；Daily 只链接剪藏并写明当天的关联或判断。
7. 不确定结论标为 `[推断]`。
8. 除非用户明确要求，否则不要删除、归档、发布或导出用户笔记。
9. 章节标题是 Dataview 统计的标识契约：今日计划、随手记录、输入、输出、生活时间线、学到、复盘、明日 / 迁移 必须与 `Templates/daily.md` 完全一致，不要改名、加前缀或加空格；行尾 `#kind/...` 标签是细粒度标识，用于文章、工具、课程等类型的分类统计。

## 活跃目录

- `Daily/`：唯一日常记录源。
- `Clippings/`：Obsidian Web Clipper 的原文快照。
- `Summaries/`：从 Daily 派生的周/月/季/年复盘与 Dataview 汇总。
- `Templates/`：Daily 和周期复盘模板。

`.dailyvault/openapi/` 是独立的本地 HTTP/MCP 支持模块；除非任务涉及 API，不要改动其中代码。

## 日常工作流

### 每日记录

QuickAdd 只提供“创建今天”和“追加到今天”。当天记录保持自然语言，使用固定章节：今日计划、随手记录、输入、输出、生活时间线、学到、复盘、明日 / 迁移。

- 今日计划：最多三项可勾选行动。
- 随手记录：白天的临时捕捉；不要求即时分类。
- 输入：文章、工具、对话、课程和反馈等外部信息，行尾带 `#kind/...` 类型标签（模板注释有速查）。
- 输出：完成的交付物、决定或明确结果。
- 生活时间线：当天经历的活动和片段，可带 `#kind/fitness`、`#kind/place` 等标签。
- 学到：从输入或经历中提炼出的认识。
- 复盘：对行动过程的评价；明天的动作放到“明日 / 迁移”。

同一事实只写一次，按其对当天最有用的意义放入一个章节。

### 每日收尾

当用户要求“daily 总结 / 收尾 / 复盘 / closeout”时，先读相应的 `Daily/YYYYMMDD.md`；只有 Daily 中有明确的 Clippings 链接时才读取对应剪藏。输出今日主线、输入、输出、计划完成情况、生活片段、学到、复盘、迁移项和明日最重要三件事建议。没有证据时写“未观察到”。

### 周期复盘与 Dataview

周/月/季/年复盘以 `Daily/` 为唯一证据源。Dataview 可以按 Daily frontmatter 的 `week`、`month`、`quarter`、`year` 汇总页面、任务和固定章节下的列表项：章节条目统计、每日活跃度、计划完成情况（今日计划 checkbox）、未完成任务、`#kind/...` 条目分类，以及“随手记录”（闪念）、“输入”、“输出”、“学到”、“复盘”、“生活时间线”的内容归集。不要从正文推断未记录的状态或指标。

复盘生成有两条路径：

- **半自动**：QuickAdd「创建周/月/季/年复盘」在 `Summaries/{period}/` 建骨架，Templater 文件夹模板自动填日期 frontmatter，Dataview 自动统计，用户只写总结文字。
- **全自动**：调用 `period-review` skill，读取该周期全部 Daily 后生成含总结内容的完整复盘。周期归属由复盘笔记 frontmatter 的 `week`/`month`/`quarter`/`year` 决定，创建时必须填准。

## 编辑策略

1. 只读取与任务相关的 Daily、模板、剪藏或汇总文件。
2. 修改模板后，确认 QuickAdd 的“追加到今天”仍定位到 `## 随手记录`。
3. 修改 JSON 配置后，确认仍能解析。
4. 不改写历史 Daily，也不恢复已退役的 Source/Note 默认流程。
